# Story 2.10.5 evidence: advice Assay can stand behind

Band 2.10, added by the product owner on 9 September 2026 reviewing the 2.10.2
preview. Recorded by the build session the same day.

---

## 1. A control file changed. Read this first.

The band's standing rule is that fixture integrity holds and a change to a
fixture is said before it is made. **This story changed two of them**, and
there was no way to do the story without it.

- `forecast-app/tests/run_bundle_fixtures/run_bundle.golden.json`
- `forecast-app/tests/run_bundle_fixtures/run_manifest.golden.json`
- their two recorded hashes in `forecast-app/tests/fixture_hashes.json`

**Why it was unavoidable.** The advice strings are engine output. The run
bundle records the engine's output verbatim, so any change to a "Do this"
sentence moves the bundle, and the manifest carries a sha256 of each stage
payload, so it moves too. The strings cannot be changed anywhere else.

**Why this is not the move the rule forbids.** Both files are derived
artefacts, regenerated by the repository's own generator,
`tests/generate_run_bundle_goldens.py`, and never edited by hand. The
generator re-verifies the quality result against `expected_quality.json`
before it writes anything, so the engine's judgements are checked against an
untouched expectation on the way through. Definition of done item 5 names
golden regeneration as a build session's work. No `expected_*.json` file and no
fixture CSV was touched, and no schema shape changed, so no version bump was
due.

**What actually moved.** The regenerated files were compared leaf by leaf
against the committed ones:

| File | Leaf values | Changed | Added | Removed |
|---|---|---|---|---|
| `run_bundle.golden.json` | 1453 before, 1453 after | 24 | 0 | 0 |
| `run_manifest.golden.json` | 210 before, 210 after | 5 | 0 | 0 |

Of the 24 in the bundle: **13 `action` strings, 3 `detail` strings**, and 8
hashes that follow from them (three stage payload digests, two content
fingerprints, the manifest digest and the bundle digest). The manifest's 5 are
all hashes.

**Nothing else moved.** No metric, band, demand class, routing decision, count,
volume share, SKU, date or threshold differs. The full list of changed leaf
paths was read out and every non-hash entry is an `action` or a `detail`.

---

## 2. The rule

> Assay reports what the data shows. Where it tells a planner to act, it may
> only tell them to look at something or ask someone. It may never tell them to
> change a number, because it does not know their lead time, service target or
> costs.

## 3. The sweep

Every advice and description string in the application was read against the
rule, not only the four the product owner named.

| Source | Strings |
|---|---|
| `validator.py`, finding actions | 46 |
| `quality_engine.py`, finding actions | 13 |
| `routing_engine.py`, decision actions | 7 |
| `classification_engine.py`, so what lines | 5 |
| `glossary.py`, definitions | 44 |
| `static/index.html`, forecast interpretation actions | 19 |
| **Total swept** | **134** |
| **Changed** | **7** |

### The seven changed

| # | Where | Why it failed the rule |
|---|---|---|
| 1 | `OUTLIER_CANDIDATE` what | "6 periods are robust outlier candidates" is analyst language |
| 2 | `OUTLIER_CANDIDATE` do this | "Review the affected periods" names no one to ask and nothing to decide |
| 3 | `model_eligible` do this | "what it is worth comes from the accuracy work in sprint 3" claims an accuracy Assay has not measured, in roadmap vocabulary a client should never read |
| 4 | `model_eligible_wide_interval` do this | Three of the four named failures: it removed the number, sized a buffer without lead time or service target, and claimed accuracy |
| 5 | `intermittent_methods` do this | "fits better than a monthly forecast" is a comparison Assay has not run |
| 6 | `discontinued_confirm_status` do this | "take it out of the master data and the stock holding as well, which is usually where the money is" changes a stock number and claims a cost Assay does not hold |
| 7 | Glossary, `Model eligible wide interval` | "use the range rather than the single number" removes the number in the glossary as well as the action |

### What they now say

**The outlier finding**, on the line the product owner reviewed, 35 of 35
periods with 6 flagged months:

> **What.** 6 months have unusual demand. No values have been removed or
> corrected.
> **So what.** A promotion, tender, stock build or data error may have changed
> the observed demand.
> **Do this.** Check the 6 flagged months with the account owner before
> changing the forecast.

**The wide interval decision:**

> 35 of 35 periods carry demand and the size swings widely, so the forecast
> number comes with a wide range around it. Use that range to review whether
> your stock buffer is adequate for your lead time and service target.

Together these are the product owner's replacement block, in place, on the case
he reviewed.

The grain is already inferred by the quality stage, so the outlier finding says
months, weeks or days rather than periods. A new `_period_noun` helper reads
that inferred grain and computes nothing.

### What was reviewed and kept

- **`policy_only` do this.** Every option it names hands the number back to the
  reader: committed volumes agreed with the customer, a lead time they agree, a
  buffer they have priced and accepted. It sets nothing.
- **`refused_data_quality` do this.** It is a data request and nothing else.
- **All 46 validator actions.** Every one is confirm, select, map, state,
  request, check or choose, about the file rather than about a demand number.
- **All 19 forecast interpretation actions.** Review, confirm, check, monitor,
  agree. "Validate confirmed receipts before changing the supply plan" defers
  the plan change to the reader, as the product owner's own sentence does.
- **The three sentences named in the brief as working.** The CV squared gloss,
  the outlier so what, and the erratic so what are byte for byte unchanged, and
  a test now asserts the last two.
- **The ADI gloss**, left as the brief instructs.

Three strings pass the rule and still read badly. They are recorded in
`docs/2.10-open-questions.md` as Q1 rather than changed, because the rule is
the test this story was given.

---

## 4. The rule is now a control, not a proofread

`forecast-app/tests/test_advice_rule.py` reads every action out of real engine
output, not out of source, and refuses two things:

- **A sizing or setting imperative.** Assay telling a planner what to do to one
  of their own figures. Sizing a buffer needs a lead time and a service target
  Assay does not hold.
- **A claim about forecast accuracy.** At the point these strings are written
  no forecast has been run.

It is deliberately narrow. CLAUDE.md section 8 warns that a banned phrase list
which fires on legitimate copy gets ignored, which is worse than none, so the
scan matches imperative forms only. A test asserts that **the product owner's
own replacement text passes**, so a future tightening that would refuse the
sentence this story exists to keep fails immediately.

Two further tests hold the ground: the five withdrawn phrases may not reappear
in any of the five files that carry copy, and the sentences the brief names as
working must survive.

---

## 5. Test evidence

Full suite, `python -m unittest discover -s tests` from `forecast-app/`:

```
Ran 263 tests in 9.436s

OK
```

256 tests before this story, 263 after. Seven new tests in
`tests/test_advice_rule.py`. Four existing tests were updated because they
asserted the wording this story withdraws: the 2.7.2 wide interval action test,
the routing substance table, and the two outlier detail assertions in
`test_quality_engine.py`.

Proving the new control can fail, each reverted afterwards:

| Break | Tests that failed |
|---|---|
| Reinstate "Size the buffer from the spread" | `test_no_advice_tells_a_planner_to_change_one_of_their_own_numbers`, `test_the_withdrawn_phrases_never_come_back` |
| Reinstate "Chasing the average here adds work and no accuracy" | `test_no_advice_claims_the_accuracy_of_a_forecast_that_has_not_run`, `test_the_withdrawn_phrases_never_come_back` |
| Put "robust outlier candidate" back on the finding | `test_the_outlier_finding_speaks_to_a_planner` |
| Reword the outlier so what the brief says to keep | `test_the_praised_sentences_are_untouched` |

The first attempt at the first break was a faulty injection that changed
nothing, and the control correctly stayed green. It is recorded here because a
fail-proof that silently tests nothing is the failure mode this exercise exists
to catch.

---

## 6. Browser evidence

Run locally against `uvicorn app:app`, fixture
`forecast-app/tests/fixtures/31_routing_portfolio.csv`, analysis date
2026-08-01, driven with Chromium. Opened the erratic line carrying an outlier
finding, which is the case the product owner reviewed. Text read from the
rendered page:

| Element | Rendered |
|---|---|
| Routing do this | `35 of 35 periods carry demand and the size swings widely, so the forecast number comes with a wide range around it. Use that range to review whether your stock buffer is adequate for your lead time and service target.` |
| Outlier what | `6 months have unusual demand. No values have been removed or corrected.` |
| Outlier so what | `A promotion, tender, stock build or data error may have changed the observed demand.` |
| Outlier do this | `Check the 6 flagged months with the account owner before changing the forecast.` |
| Decision gloss | `This line can be forecast, and the number comes with a wide range around it. The number is still what you plan from, and the range shows how far it could move.` |
| Uncaught page errors | none |

---

## 7. A regression fixed on the way

The 2.10.4 evidence file named a routing field as a three part dotted path,
which the committed credential scan reads as a fully qualified BigQuery table
with a literal project. That commit went out with a red suite because the
evidence file was written after the suite was run. The scanner is right to be
strict, so the prose changed rather than the pattern, and the fix is committed
on the 2.10.4 branch where the defect was introduced.

## 8. Not run

No Vercel Preview and no Production check has been run for this story.
