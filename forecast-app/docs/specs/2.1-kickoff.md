# Kickoff for the build session

Paste the block below into the new Claude Code chat as the first message, with these files attached or already committed to the repo:

- `CLAUDE.md` at the repository root
- `fixtures/30_classification_portfolio.csv`
- `fixtures/expected_classification.json`
- `docs/briefs/2.1-build-brief.md`
- `docs/briefs/2.1-fixture-note.md`

Everything the builder needs is in those five files. Do not explain the project in your own words first. If the pack is not enough on its own, that is a defect in the pack and I would rather find it now than paper over it.

---

## The message

> You are the developer on Silurian Assay. Read `CLAUDE.md` first: it carries the standing rules, the roles and the things a build session must never do.
>
> Build story 2.1 from `docs/briefs/2.1-build-brief.md`, revision 1.2, which is approved. `docs/briefs/2.1-fixture-note.md` explains the fixture. `fixtures/expected_classification.json` v1.2 is authoritative for thresholds and expected values.
>
> Before writing any code, do three things:
>
> 1. Confirm the two fixture hashes match the values recorded in the brief.
> 2. Add the fixture integrity test described in `CLAUDE.md` section 3, so that any later change to a fixture or an expectations file fails the build. Do this first, not last.
> 3. Put anything you disagree with, anything ambiguous and anything you think is wrong in `docs/2.1-open-questions.md`, in the format: what it blocks, your recommended default, cost if wrong. Then start building. Do not wait for answers on questions that have a workable default.
>
> Acceptance is the eighteen criteria in section 9 of the brief. Criterion 10 is the one to read twice: the fixture provokes six quality findings and a caveated portfolio band, and all of them are correct. If a test fails because of one of those findings, the fix is never to change the fixture, the expectations file or a quality threshold.
>
> Work on a branch and open a PR. In the PR description, state which acceptance criteria you verified and how, and name anything you could not verify.

---

## What to send back here

When the PR is up, send me:

1. The PR description.
2. `docs/2.1-open-questions.md`.
3. The actual output of the run on `30_classification_portfolio.csv`: the per SKU table, the class counts, the ABC counts and the portfolio band.

I will check it against the expectations file rather than reading the diff, then you run the deployed site yourself before we close the story. That order matters. The two defects that cost us revisions were both cases of someone reasoning about behaviour instead of running it.
