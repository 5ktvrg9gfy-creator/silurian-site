[project]
name = "silurian-forecast-diagnostic"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.116.1",
  "python-multipart==0.0.20",
]

[tool.vercel]
entrypoint = "app:app"

